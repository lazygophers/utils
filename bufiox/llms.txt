# bufiox

bufio 扩展包，提供自定义分割扫描器和跨平台行扫描器。

## 功能

1. **ScanBy** - 按指定字节序列分割数据，支持自定义分隔符
2. **ScanLines** - 行扫描器，兼容 CRLF（Windows）和 LF（Unix）换行符
3. **dropCR** - 移除字节切片末尾的回车符

## 设计

与 `bufio.Scanner` 接口兼容，可直接用作 `SplitFunc`。

## 测试

- 100% 代码覆盖率
- 31 个测试用例
