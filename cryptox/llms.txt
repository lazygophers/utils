# cryptox

加解密 / 哈希 / 签名 / 密钥派生 / 唯一 ID 工具集，封装标准库 `crypto/*` 与 `golang.org/x/crypto`。
导入路径：`github.com/lazygophers/utils/cryptox`

## 功能

一站式密码学工具包，覆盖对称加密、非对称加密、哈希、消息认证、密钥派生、密钥交换、数字签名与唯一 ID 生成：

- **AES-256 对称加密**：推荐 `Encrypt`/`Decrypt`（GCM 认证加密，含 GCM 实例缓存优化）；另提供 CBC/CFB/CTR/OFB 五种分组模式。ECB 模式已标记 `Deprecated`（相同明文块产生相同密文块，泄露结构信息）。
- **RSA 非对称加密与签名**：密钥对生成 + PEM 编解码 + OAEP/PKCS1v15 加密 + PSS/PKCS1v15 签名。
- **ECDSA 椭圆曲线签名**：P-256/P-384/P-521 曲线，签名结果 `(r, s)` 或 ASN.1 DER 字节串互转，PEM 编解码。
- **ECDH 密钥交换**：共享密钥协商 + 基于哈希的 KDF 派生 + 公钥坐标互转。
- **哈希**：MD5/SHA 系列（泛型输入，返回 hex 字符串）、CRC32/CRC64（返回整数）、FNV-1/FNV-1a（32/64 位整数）。
- **HMAC**：MD5/SHA1/SHA256/SHA384/SHA512 消息认证码（返回 hex 字符串）。
- **KDF 密钥派生**：PBKDF2-HMAC-SHA256/SHA512，附三档预设配置（Default/Fast/Strong）。
- **唯一 ID**：ULID（含时间戳提取）、UUID v4。

约束与注意：

- `Encrypt`/`Decrypt`/ECB/CBC/CFB/CTR/OFB 的 key 必须为 **32 字节**（AES-256），否则返回 `invalid key length` 错误。
- GCM 缓存以 `string(key)` 为键全局缓存 `cipher.AEAD` 实例，重复密钥场景显著降低分配；密钥不应频繁变化以免缓存膨胀。
- 哈希 / HMAC / FNV 函数为泛型 `[M string | []byte]`，可直接传 string 或 []byte。
- 内部含 `newCipherFunc`/`newGCMFunc`/`randReader`/`ecdhRandReader`/`ecdsaRandReader` 等可注入变量，仅供测试，非公共 API。

## 快速开始

```go
package main

import (
	"crypto/elliptic"
	"crypto/rand"
	"crypto/sha256"
	"fmt"

	"github.com/lazygophers/utils/cryptox"
)

func main() {
	// AES-256-GCM 认证加密（推荐）
	key := make([]byte, 32)
	rand.Read(key)
	ct, _ := cryptox.Encrypt(key, []byte("hello"))
	pt, _ := cryptox.Decrypt(key, ct)
	fmt.Println(string(pt)) // hello

	// 哈希（泛型输入，返回 hex 字符串）
	fmt.Println(cryptox.Sha256("hello"))
	fmt.Println(cryptox.Md5([]byte("hello")))

	// HMAC
	fmt.Println(cryptox.HMACSHA256("secret-key", "message"))

	// PBKDF2 密钥派生
	salt := make([]byte, 16)
	rand.Read(salt)
	dk := cryptox.PBKDF2SHA256([]byte("password"), salt, 100000, 32)

	// RSA 加密 + 签名
	kp, _ := cryptox.GenerateRSAKeyPair(2048)
	enc, _ := cryptox.RSAEncryptOAEP(&kp.PrivateKey.PublicKey, []byte("data"))
	dec, _ := cryptox.RSADecryptOAEP(kp.PrivateKey, enc)
	_ = dec

	// ECDSA 签名
	ek, _ := cryptox.GenerateECDSAP256Key()
	r, s, _ := cryptox.ECDSASignSHA256(ek.PrivateKey, []byte("data"))
	ok := cryptox.ECDSAVerifySHA256(ek.PublicKey, []byte("data"), r, s)
	fmt.Println(ok)

	// ECDH 密钥交换
	alice, _ := cryptox.GenerateECDHP256Key()
	bob, _ := cryptox.GenerateECDHP256Key()
	shared, _ := cryptox.ECDHKeyExchange(alice.PrivateKey, bob.PublicKey, 32)
	_ = shared

	// 唯一 ID
	fmt.Println(cryptox.ULID())
	fmt.Println(cryptox.UUID())

	_ = elliptic.P256
	_ = sha256.New
	_ = dk
}
```

## 核心类型

```go
// RSA 密钥对
type RSAKeyPair struct {
	PrivateKey *rsa.PrivateKey
	PublicKey  *rsa.PublicKey
}
func (kp *RSAKeyPair) PrivateKeyToPEM() ([]byte, error)
func (kp *RSAKeyPair) PublicKeyToPEM() ([]byte, error)

// ECDSA 密钥对
type ECDSAKeyPair struct {
	PrivateKey *ecdsa.PrivateKey
	PublicKey  *ecdsa.PublicKey
}

// ECDSA 签名（ASN.1 结构）
type ECDSASignature struct {
	R, S *big.Int
}

// ECDH 密钥对（结构同 ECDSA，用于密钥交换）
type ECDHKeyPair struct {
	PrivateKey *ecdsa.PrivateKey
	PublicKey  *ecdsa.PublicKey
}

// PBKDF2 配置
type PBKDF2Config struct {
	Iterations int              // 迭代次数，建议 ≥ 100000
	SaltLen    int              // 盐值长度，建议 ≥ 16
	KeyLen     int              // 派生密钥长度
	HashFunc   func() hash.Hash // 哈希函数，默认 sha256.New
}
```

## 主要 API

### AES（key 必须 32 字节）

```go
func Encrypt(key, plaintext []byte) ([]byte, error)   // AES-256-GCM，推荐
func Decrypt(key, ciphertext []byte) ([]byte, error)
func EncryptECB(key, plaintext []byte) ([]byte, error) // Deprecated：不安全
func DecryptECB(key, ciphertext []byte) ([]byte, error)
func EncryptCBC(key, plaintext []byte) ([]byte, error)
func DecryptCBC(key, ciphertext []byte) ([]byte, error)
func EncryptCFB(key, plaintext []byte) ([]byte, error)
func DecryptCFB(key, ciphertext []byte) ([]byte, error)
func EncryptCTR(key, plaintext []byte) ([]byte, error)
func DecryptCTR(key, ciphertext []byte) ([]byte, error)
func EncryptOFB(key, plaintext []byte) ([]byte, error)
func DecryptOFB(key, ciphertext []byte) ([]byte, error)
```

### 哈希（泛型 `[M string | []byte]`，返回 hex 字符串）

```go
func Md5[M](s M) string
func SHA1[M](s M) string
func Sha224[M](s M) string
func Sha256[M](s M) string
func Sha384[M](s M) string
func Sha512[M](s M) string
func Sha512_224[M](s M) string
func Sha512_256[M](s M) string
```

### CRC / FNV（泛型输入，返回整数）

```go
func CRC32[M](s M) uint32    // IEEE 多项式
func CRC64[M](s M) uint64    // ECMA 多项式
func Hash32[M](s M) uint32   // FNV-1 32 位
func Hash32a[M](s M) uint32  // FNV-1a 32 位
func Hash64[M](s M) uint64   // FNV-1 64 位
func Hash64a[M](s M) uint64  // FNV-1a 64 位
```

### HMAC（泛型输入，返回 hex 字符串）

```go
func HMACMd5[M](key, message M) string
func HMACSHA1[M](key, message M) string
func HMACSHA256[M](key, message M) string
func HMACSHA384[M](key, message M) string
func HMACSHA512[M](key, message M) string
```

### KDF

```go
func PBKDF2SHA256(password, salt []byte, iterations, keyLen int) []byte
func PBKDF2SHA512(password, salt []byte, iterations, keyLen int) []byte
func DeriveKey(password, salt []byte, iterations, keyLen int, hashFunc func() hash.Hash) []byte
func DefaultPBKDF2Config() PBKDF2Config // 100000 迭代 / 32B 盐 / 32B 密钥 / SHA-256
func FastPBKDF2Config() PBKDF2Config    // 50000 迭代 / 16B 盐 / 32B 密钥 / SHA-256
func StrongPBKDF2Config() PBKDF2Config  // 200000 迭代 / 64B 盐 / 64B 密钥 / SHA-512
```

### RSA

```go
func GenerateRSAKeyPair(keySize int) (*RSAKeyPair, error)
func PrivateKeyFromPEM(pemData []byte) (*rsa.PrivateKey, error)
func PublicKeyFromPEM(pemData []byte) (*rsa.PublicKey, error)
func RSAEncryptOAEP(publicKey *rsa.PublicKey, plaintext []byte) ([]byte, error)
func RSADecryptOAEP(privateKey *rsa.PrivateKey, ciphertext []byte) ([]byte, error)
func RSAEncryptPKCS1v15(publicKey *rsa.PublicKey, plaintext []byte) ([]byte, error)
func RSADecryptPKCS1v15(privateKey *rsa.PrivateKey, ciphertext []byte) ([]byte, error)
func RSASignPSS(privateKey *rsa.PrivateKey, message []byte) ([]byte, error)
func RSAVerifyPSS(publicKey *rsa.PublicKey, message, signature []byte) error
func RSASignPKCS1v15(privateKey *rsa.PrivateKey, message []byte) ([]byte, error)
func RSAVerifyPKCS1v15(publicKey *rsa.PublicKey, message, signature []byte) error
func GetRSAKeySize(key *rsa.PublicKey) int
func RSAMaxMessageLength(publicKey *rsa.PublicKey, padding string) (int, error)
```

### ECDSA

```go
func GenerateECDSAKey(curve elliptic.Curve) (*ECDSAKeyPair, error)
func GenerateECDSAP256Key() (*ECDSAKeyPair, error)
func GenerateECDSAP384Key() (*ECDSAKeyPair, error)
func GenerateECDSAP521Key() (*ECDSAKeyPair, error)
func ECDSASign(privateKey *ecdsa.PrivateKey, data []byte, hashFunc func() hash.Hash) (r, s *big.Int, err error)
func ECDSASignSHA256(privateKey *ecdsa.PrivateKey, data []byte) (r, s *big.Int, err error)
func ECDSASignSHA512(privateKey *ecdsa.PrivateKey, data []byte) (r, s *big.Int, err error)
func ECDSAVerify(publicKey *ecdsa.PublicKey, data []byte, r, s *big.Int, hashFunc func() hash.Hash) bool
func ECDSAVerifySHA256(publicKey *ecdsa.PublicKey, data []byte, r, s *big.Int) bool
func ECDSAVerifySHA512(publicKey *ecdsa.PublicKey, data []byte, r, s *big.Int) bool
func ECDSAPrivateKeyToPEM(privateKey *ecdsa.PrivateKey) ([]byte, error)
func ECDSAPrivateKeyFromPEM(pemData []byte) (*ecdsa.PrivateKey, error)
func ECDSAPublicKeyToPEM(publicKey *ecdsa.PublicKey) ([]byte, error)
func ECDSAPublicKeyFromPEM(pemData []byte) (*ecdsa.PublicKey, error)
func ECDSASignatureToBytes(r, s *big.Int) ([]byte, error)         // ASN.1 DER
func ECDSASignatureFromBytes(data []byte) (r, s *big.Int, err error)
func GetCurveName(curve elliptic.Curve) string
func IsValidCurve(curve elliptic.Curve) bool
```

### ECDH

```go
func GenerateECDHKey(curve elliptic.Curve) (*ECDHKeyPair, error)
func GenerateECDHP256Key() (*ECDHKeyPair, error)
func GenerateECDHP384Key() (*ECDHKeyPair, error)
func GenerateECDHP521Key() (*ECDHKeyPair, error)
func ECDHComputeShared(privateKey *ecdsa.PrivateKey, publicKey *ecdsa.PublicKey) ([]byte, error)
func ECDHComputeSharedWithKDF(privateKey *ecdsa.PrivateKey, publicKey *ecdsa.PublicKey, keyLength int, kdf func() hash.Hash) ([]byte, error)
func ECDHComputeSharedSHA256(privateKey *ecdsa.PrivateKey, publicKey *ecdsa.PublicKey, keyLength int) ([]byte, error)
func ECDHKeyExchange(alicePrivateKey *ecdsa.PrivateKey, bobPublicKey *ecdsa.PublicKey, keyLength int) ([]byte, error)
func ValidateECDHKeyPair(keyPair *ECDHKeyPair) error
func ECDHPublicKeyFromCoordinates(curve elliptic.Curve, x, y *big.Int) (*ecdsa.PublicKey, error)
func ECDHPublicKeyToCoordinates(publicKey *ecdsa.PublicKey) (x, y *big.Int, err error)
func ECDHSharedSecretTest(keyPair1, keyPair2 *ECDHKeyPair) (bool, error)
```

### ULID / UUID

```go
func ULID() string
func ULIDWithTimestamp() (string, int64)
func GetULIDTimestamp(id string) (int64, error)
func MustGetULIDTimestamp(id string) int64
func UUID() string // UUID v4
```

## 文件结构

| 文件 | 职责 |
| --- | --- |
| `aes.go` | AES-256 GCM/ECB/CBC/CFB/CTR/OFB 加解密 + GCM 缓存 + PKCS7 填充 |
| `rsa.go` | RSA 密钥对、PEM 编解码、OAEP/PKCS1v15 加密、PSS/PKCS1v15 签名 |
| `ecdsa.go` | ECDSA 密钥对、签名/验签、PEM 与 ASN.1 DER 编解码、曲线工具 |
| `ecdh.go` | ECDH 密钥对、共享密钥协商、基于哈希的 KDF、公钥坐标互转 |
| `hash_basic.go` | MD5 / SHA1 / SHA224 / SHA256 / SHA384 / SHA512 / SHA512-224 / SHA512-256 |
| `hash_crc.go` | CRC32（IEEE）/ CRC64（ECMA） |
| `hash_fnv.go` | FNV-1 / FNV-1a 32 位与 64 位哈希 |
| `hash_hmac.go` | HMAC-MD5 / SHA1 / SHA256 / SHA384 / SHA512 |
| `kdf.go` | PBKDF2-SHA256/SHA512 派生 + Default/Fast/Strong 预设配置 |
| `ulid.go` | ULID 生成与时间戳提取 |
| `uuid.go` | UUID v4 生成 |

## AES 模式对比

| 模式 | 函数 | 认证 | 安全性 | 备注 |
| --- | --- | --- | --- | --- |
| GCM | `Encrypt`/`Decrypt` | 是 | 高 | 推荐；带 GCM 实例缓存 |
| CBC | `EncryptCBC`/`DecryptCBC` | 否 | 中 | 需配合 PKCS7 填充 |
| CTR | `EncryptCTR`/`DecryptCTR` | 否 | 中 | 流式 |
| CFB | `EncryptCFB`/`DecryptCFB` | 否 | 中 | 流式 |
| OFB | `EncryptOFB`/`DecryptOFB` | 否 | 中 | 流式 |
| ECB | `EncryptECB`/`DecryptECB` | 否 | 低 | **Deprecated**，泄露明文结构 |
